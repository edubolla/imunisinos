# Handoff — LP Ads Imunisinos

Atualizado em 19/08/2026.

## Onde está

Oficial: https://github.com/edubolla/lpimunisinos (`main`).  
Backup neste repo: pasta `LP/`.

```
insetos/   ratos/   cupins/   cip/
css/       js/      images/{marca,equipe,insetos,ratos,cupins,cip,campo}
```

## Commit e deploy pelo Cursor

Este ambiente (`imunisinos`) **não** consegue push no `lpimunisinos`.

1. GitHub → Settings → Applications → Cursor → Configure → incluir `lpimunisinos`.
2. Vercel: projeto **novo** importando `lpimunisinos`, framework Other, sem build.
3. Pedir edições com o Cursor **aberto no `lpimunisinos`**.

## Regras

- HTML/CSS/JS. Sem Next, sem Imuni chat.
- WhatsApp só Tintim.
- Não apontar Google Ads para Vercel.

Contexto Ads: `docs/ADS-CONTINUIDADE.md` no repo da Imuni.
